import 'package:flutter/widgets.dart';

class UserInfoBasePage extends StatelessWidget {
  const UserInfoBasePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('UserBasePage'),
    );
  }
}
