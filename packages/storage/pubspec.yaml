name: storage
description: storage package
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  models:
    path: ../models

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.8
  very_good_analysis: ^6.0.0
