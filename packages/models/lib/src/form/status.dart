/// [FormStatus] is an enum class that contains the status of the form.
enum FormStatus {
  /// [initial] status
  initial,

  /// [loading] status
  loading,

  /// [success] status
  success,

  /// [failure] status
  failure,
}
